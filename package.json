{
  "name": "multica-apple-reminders-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "macOS Menu Bar bridge between Multica Cloud and Apple Reminders",
  "scripts": {
    "deploy": "./scripts/deploy.sh"
  }
}
